{
  "name": "collection-platform-browser-extension",
  "version": "4.0.1",
  "private": true,
  "description": "Adapter-driven knowledge acquisition runtime with the Discord reference adapter.",
  "scripts": {
    "check": "node tests/static-check.js && node --test tests/*.test.js",
    "check:syntax": "node tests/static-check.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
